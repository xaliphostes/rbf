{
    "name": "rbf-interpolator-visualizer",
    "version": "1.0.0",
    "description": "Interactive 2D visualization of RBF interpolation with thin plate spline",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "devDependencies": {
        "typescript": "^5.3.3",
        "vite": "^5.0.10",
        "gh-pages": "^6.3.0"
    }
}
